<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <!--
                    <span>
                        <img alt="image" class="img-circle" src="<%= image_url 'profile_small.png' %>"/>
                    </span>                    
                    -->
                    
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold">윗더베 리그</strong>
                            </span> <span class="text-muted text-xs block">장섭독수리 팀<b class="caret"></b></span>
                        </span>
                    </a>
                    
                    
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li><a href="#">Logout</a></li>
                        <!-- 여기에 더 추가 가능 -->
                    </ul>
                </div>
            </li>
            <li class="<%= is_active_controller('league') %>">
                <%= link_to league_info_path do %>
                <i class="fa fa-laptop"></i> <span class="nav-label">리그 설정</span>
                <% end %>
            </li>
            <li class="<%= is_active_controller('league') %>">
                <a href="#"><i class="fa fa-desktop"></i> <span class="nav-label">마이팀</span><span class="fa arrow"></a>
                <ul class="nav nav-second-level collapse <%= is_active_controller('league') %>">
                    <li class="<%= is_active_action('lineup') %>"><%= link_to "라인업", league_lineup_path %></li>
                    <li class="<%= is_active_action('trade') %>"><%= link_to "트레이드", league_trade_path %></li>
                </ul>
            </li>
        </ul>

    </div>
</nav>